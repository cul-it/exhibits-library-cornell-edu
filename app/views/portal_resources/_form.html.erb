<%= form_for([current_exhibit, @resource.becomes(PortalResource)]) do |f| %>
  <%= f.text_field :url  %>
  <%= f.submit t('.add_item'), class: 'btn btn-primary' %>
<% end if can? :manage, PortalResource %>
