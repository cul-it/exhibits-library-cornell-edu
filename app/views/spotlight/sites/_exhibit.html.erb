<% exhibit = f.object %>
<tr class="dd-item dd3-item" data-id="<%= exhibit.id %>">
  <td>
     <div class="handle-wrap">
       <div class="dd-handle dd3-handle"><%= t :drag %></div>

       <%= f.hidden_field :id %>
       <%= f.hidden_field :weight, data: { property: "weight" } %>

       <%= link_to exhibit.title, exhibit %>
     </div>

  </td>
  <td class="text-right text-end"><%= exhibit.published? ? t(:'.published') : t(:'.unpublished') %></td>
  <%# BEGIN CUSTOMIZATION - Add published date column %>
  <td class="text-right text-end"><%= display_exhibit_published_date(exhibit) %></td>
  <%# END CUSTOMIZATION %>
  <td class="text-right text-end"><%= exhibit.requested_by %></td>
  <td class="text-right text-end"><%= l exhibit.created_at, format: :long %></td>
  <td class="text-right text-end"><%= l exhibit.updated_at, format: :long %></td>
</tr>
